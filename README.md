# Github-Action-Workflow
##Github-Action-Workflow STEPS

EVENT (PUSH, PULL, MERGE)
JOB (What action should be perfromed when event happened -> SCRIPTS)
STEP (Steps in the scripts -> Single task is called step)
Runner (Virtual Machine where we run our jobs)

Path :- Directory/.github/workflows/xyz.yml
